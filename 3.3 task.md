```
1. Делается вызов chdir 
chdir("/tmp")

2. stat("/etc/magic", {st_mode=S_IFREG|0644, st_size=111, ...}) = 0?

3. cat /dev/null > logfilename.log

4. Зомби процессе не потребляют ресурс, так как они уже отработали, но их код выхода не был обработан корректно. 

5. 
```